λ𝛼.λx.
  { fact ← [(*)/ₒ 1 (1..x)]
  ; f10 ← λa.λx.
    { rf ← [(*)/ₒ 1 (𝒻 x (x+y-1) (⌊y))]; ffact ← rf 1
    ; Σ ← λlb.λub.λa. (+)/ₒ 0 (a'lb..ub)
    ; Σ 0 30 (λk. {kk⟜ℝ k; (x^k%rf a kk)%ffact kk})
    }
  ; ((x%2)^𝛼%ℝ(fact 𝛼))*f10 (ℝ(𝛼+1)) (_(x^2%4))
  }
