λn.
  { ni ⟜ ⌊(√(ℝn))
  ; pns ← ⍳ 2 ni
  ; isPrime ← λn.¬((∨)/ₒ #f ([n|x=0]'⍳ 2 (⌊(√(ℝn))))); pf ⇐ (isPrime #.)
  ; pps ⟜  (λk. n|k=0) #. pns
  ; ?ni^2=n
    ,.pf (pps⧺(n/.)'}:?pps)
    ,.pf (pps⧺(n⊳((n/.)'pps)))
  }
