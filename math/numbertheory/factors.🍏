λn.
  { ni ⟜ ⌊(√(ℝn))
  ; isPrime ← λn.¬((∨)/ₒ #f ([n|x=0]'2..(⌊(√(ℝn))))); pf ⇐ (isPrime #.)
  ; pps ⟜  (λk. n|k=0) #. 2..ni
  ; ?ni^2=n
    ,.pf (pps⧺(n/.)'}:?pps)
    ,.pf (pps⧺(n⊳((n/.)'pps)))
  }
