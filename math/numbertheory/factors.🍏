λn.
  { ni ⟜ ⌊(√(ℝn))
  ; pns ← (⍳ 2 ni 1)
  ; isPrime ← λn.¬((∨)/ₒ #f ([(n|x)=0]'(⍳ 2 (⌊(√(ℝn))) 1))); pf ⇐ (isPrime #.)
  ; pps ⟜ pf pns
  ; ?ni^2=n
    ,.pf ((/.n)'(}:? pps))⧺pps
    ,.pf (n⊲(/.n)'pps)⧺pps
  }
