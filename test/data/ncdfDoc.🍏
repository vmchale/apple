λz.
{
  erf ← λz.
    {
      facts ⟜ (*) Λₒ 1 (frange 1 30 30);
      Σ ← λN.λa. (+)/ₒ 0 (a'⍳N);
      (2%√𝜋)*Σ 30 (λn. ((_1^n)*z^(2*n+1))%(facts˙n*(2*ℝn+1)))
    };
  zz ⟜ z%(√2);
  0.5*(1+erf(zz))
}
