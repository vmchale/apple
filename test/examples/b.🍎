\xs.\ys.
{
  -- FIXME: this fails with polymorphic bind lol
  Σ ← [(+)/1 0 x];
  n ⟜ 𝑖(:xs);
  xbar ⟜ (Σ xs) % n;
  ybar ← (Σ ys) % n;
  xy ← Σ ((*)`2 1 xs ys);
  x2 ← Σ ((^2)'1 xs);
  denom ← (x2-(n*(xbar^2)));
  (xy-(n*xbar*ybar))%denom
}
