λa.λb.λz.
{
  rf ← [(*)/1 1 (frange x (x+y-1) (⌊y))]; fact ← rf 1;
  Σ ← λN.λa. (+)/1 0 (a'1 (irange 0 N 1));
  sum ← [(+)/1 0 x]; term ← λn. {nn⟜itof n; (sum ((%)`((λa.rf a nn)'1 a) ((λb. rf b nn)'1 b)))*((z^n)%(fact nn))};
  Σ 99 term
}
