-- enumerate permutations in reduced form
-- https://dl.acm.org/doi/10.1145/114054.114077
λn.
{
  fact ← [(*)/ₒ 1 (1..x)];
  antibase ← λk.λbs. (->1)'({: ((λqr.λb. {s ⟜ qr->2; (s|b, s/.b)}) Λₒ (0,k) bs));
    bs ⟜ 1..n;
  (λk. ~(antibase k bs))'⍳ (fact n-1)
}
