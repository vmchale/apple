λA.
{
  N ← 𝓉A; 
  n ⟜ ℝN; C ⟜ 2*𝜋%n;
  c ← λk. (cos. (C*k), sin. (C*k));
  add ← λ(r₀,i₀).λ(r₁,i₁). (r₀+r₁,i₀+i₁);
  mul ← λ(r₀,i₀).λ(r₁,i₁). (r₀*r₁-i₀*i₁,r₀*i₁+r₁*i₀);
  (λj. (add/(mul`(A::Vec n (𝞈,𝞈)) ((λk. c (j*k))'frange 0 (n-1) N))))'frange 0 (n-1) N
}
