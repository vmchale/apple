\documentclass{article}

\usepackage{fontspec}
\usepackage{hyperref}
\usepackage{fancyvrb}

\begin{document}

\VerbatimFootnotes
\setmonofont{Jet Brains Mono}[Scale=MatchAveragecase]

\author{V. E. McHale}
\title{Compiling Array Languages for SIMD}
\maketitle

\begin{abstract}
  The C compilation model has dominated compilers literature but it opposes Single-Intruction, Multiple Data (SIMD). We articulate the assumptions behind textbook methods, where new CPUs diverge, and give a first cut of a better approach used in the Apple Array System. By reconsidering expectations about compiler optimization, we give a program for high-level languages to offer performant SIMD. Array languages turn out to be an especially good candidate, expressing efficient operations in a high-level language; we give examples in Apple.
\end{abstract}

\section{Background}

\subsection{Textbook Compilation}

C implementations compile the language's constructs (functions) to sequences of steps in assembly which read and write registers for arguments and return values defined by convention. The correspondence is elegant \footnote{Kell \cite{kell2017} points out that C is uniquely suited to interfacing with other languages, in part because of this compilation practice.}, but not suited to SIMD.

Consider a compiler written as mutually recursive functions:

\begin{verbatim}
writeExpr :: Expr -> Temp -> [Stmt]

writeFn :: Expr -> [Temp] -> Temp -> [Stmt]
\end{verbatim}

The first function takes an expression and a register and creates a series of statements that will write that its value to the given register. The second takes an expression representing a function, registers to read arguments from, and a register to write the return value, and returns a series of statements. This is used in Appel \cite{appel1998}[pp.~148-169], for instance.

Na\"ively\footnote{This was the approach taken in the Apple array system at first.}, we might try to compile \verb|λas. map (λx. f x) as|, which maps over a vector, as follows:

\begin{itemize}
  \item Generate temporaries \verb|x|, \verb|y| for the argument and return value of \verb|f|, respectively.
  \item Compile \verb|λx. f x| using \verb|writeFn|.
  \item Generate code to loop over the input vector's elements, reading from the input and putting the value in \verb|x|, and then storing the result \verb|y| in the output vector.
\end{itemize}

This is appealing because of its simplicity, and also has the advantage that \verb|f| can be defined in another language. However in the case of

\begin{verbatim}
λas. map (λx. x*2) as
\end{verbatim}

it would step over the input vector's elements one at a time. This is not as efficient as it could be---SIMD instructions can multiply several elements at once. Thus compiling \verb|map| should not resort to a single canonical compiled version of \verb|f|.

% https://fabiensanglard.net/dc/

\subsection{Functions in Theory}

Landin \cite{landin1965} points out that Algol corresponds to the lambda calculus. Functions in the lambda calculus have a formal definition and are applicable to programming in practice, but only some have efficient SIMD implementations: \verb|λx. 1-x^2| and \verb|if x≥0 then x else 0| are both functions but the latter involves a conditional which in general is compiled to a jump; they must be treated differently during compilation but are not distinguished in the source language. % perhaps why programmers find it fickle!

% https://www.netlib.org/cephes/doubldoc.html#log

Cataloguing which functions can be compiled with vector instructions would be a welcome contribution to the compilers literature.

% Cataloguing + maybe expanding (branchless already studied?)

% http://blog.vmchale.com/article/intuitionist-c

\subsection{Limits of C}

What makes C special in practice is that functions, which can be composed, are compiled to sequences of assembly instructions stored in object files; compositionality is borne through until linking. This compositionality is at odds with SIMD, where a high-level function \verb|λx. x*2| should be compiled using different instructions depending on whether it is lifted to operate on an array. macOS provides

\begin{verbatim}
     float expf(float x);
     vFloat vexpf(vFloat x);
     void vvexpf(float *y, const float *x, const int *n);
\end{verbatim}

as three shared library functions to address this.
A library providing a sigmoid function (for instance) would need to expose three versions in a shared library. The clumsiness proliferates and the mathematical entity no longer corresponds to compiled code but rather three versions of itself depending on the context in which it is used.

A compiler-as-a-library is appealing because it could be invoked by another compiler, dispatching different versions of a function without intervention from the programmer.

% actually the problem with C++ is more that compilers are fickle, clang vs. gcc https://marek.ai/matrix-multiplication-on-cpu.html

\section{Efficient Compilation}

% also phase-optimization problem... examples where compilation techniques allow XYZ (phase optimization problem cited for entrenchment of clang/gcc but maybe we should compile different things! LLVM is great but not always a target, e.g. laziness...)
The full employment theorem for compiler writers states that it is impossible to write an algorithm that transforms an arbitrary program into an optimal version. Of course, such a ``big hammer'' would be handy, but the problem sitting before us is narrower. A compiler ferries a (possibly high-level) language to efficient code, and actually a source language with fewer low-level facilities offers more invariants. ``Optimization'' writ large gets much attention, but transformations that make high-level languages possible have not entered the computer science canon, particularly for functional or exotic languages.

``Amenability to efficient compilation'' also informs the design of the source language. One can also consider languages where the programmer can expect efficient compilation by reasoning about the source-language in its own terms. This is not the case for C++, where efficiency of generated code can vary dramatically by compiler or across versions of the same compiler \cite{intrinsics,kolodiej,cc}. There is no contract with the user; performance depends on internals that are not documented, justified, analyzed with the same scrutiny as programming languages.

Array languages have a chance to do better since they naturally express computations that can be executed via SIMD, in a domain where such CPU features offer significant gains.
% C++ is "compiler expert" and the internals have no contract w/ user... (despite focus on performance->C++ people)
% esp. problem with numeric code (see GCC example) where SIMD offers real performance
% https://danluu.com/assembly-intrinsics/
% "predictable how to get efficient code (from the source)" in addition to "high-level efficiently" (still useful b/c that's better than writing assembly...; C++ compilers very much do not do this)

% IR for interprocedural analysis is mentioned by Appel, actually! llvm puts its IR in object files

\subsection{Typing \& Indexing}

% neither indices nor bounds are associated with the array

% pragmas LOL
\begin{verbatim}
float* matmul(float* a, float* b, int m, int n, int l) {
    float* c=malloc(m*l*sizeof(float));
    for (int i=0; i<m; i++) {
        for (int j=0; j<n; j++) {
            float z=0;
            for (int k=0; k<l; k++) {
                z+=A[i*m+k]*B[k*l+j];
            }
            C[i*m+j]=z;
        }
    }
    return c;
}
\end{verbatim}

% indexing by for-loops is elegant but index information (parity!) doesn't propagate so easily (optimization is "internal" rather than specified system)

Suppose we wish to compute the 7-day moving average. We will do so using \verb|`\|, inspired by J's infix \cite{nuvoc}, which lifts a vector function to operate on windows of 7-vectors, arranging the (scalar) results into a vector. Examining type annotations, we see that the fold of the inner loop (\verb|/|) is inferred have type \verb|(float → float → float) → Vec 7 float → float|:

\begin{verbatim}
 > :ann λxs. [(+)/x%ℝ(:x)]\`7 xs
λ(xs : Vec (i + 7) float).
    ((\`7 : (Vec 7 float → float) → Vec (i + 7) float → Vec (i + 1) float)
     λ(x : Vec 7 float).
         ((% : float → float → float)
          ((/ : (float → float → float) → Vec 7 float → float)
           (+ : float → float → float)
           (x : Vec 7 float))
          ((ℝ : int → float) ((: : Vec 7 float → int) (x : Vec 7 float))))
     (xs : Vec (i + 7) float))
\end{verbatim}

and in fact the inner loop is compiled without

\begin{verbatim}
 > :asm λxs. [(+)/x%ℝ(:x)]\`7 xs

    ⋮
    ldr d2, [sp, x2, LSL #3]
    fadd d5, d5, d2
    add x4, x4, #0x1
apple_1:
    mov x2, #0x10
    add x2, x2, x4, LSL #3
    ldr q2, [sp, x2]
    fadd v3.2d, v3.2d, v2.2d
    add x4, x4, #0x2
    cmp x4, x3
    b.LT apple_1
    faddp d2, v3.2d
    fadd d5, d5, d2
    ⋮
\end{verbatim}

Similar analyses are already performed by industrial compilers, but types are part of the source language and thus available to any programmer who knows the language---contrast autovectorization in C++, which is a matter for compiler experts. Moreover, type systems, unlike compiler internals, are not subject to change between versions.

In a type system with shapes, information proliferates (based on rules)

\section{A First Cut at a Solution for the Apple Array System}

Apple adds functions

\begin{verbatim}
write2 :: Expr -> Temp2 -> [Stmt]

writeFn2 :: Expr -> [Temp2] -> Temp2 -> [Stmt]
\end{verbatim}

which compile expressions such as \verb|λx. x*2| to statements operating on vector registers.

Then, when compiling, say, \verb|map f|, we use

\begin{verbatim}
hasSIMD :: Expr -> Bool
\end{verbatim}

to determine whether \verb|f| can operate on vector registers.

\verb|hasSIMD| traverses the AST, returning \verb|False| when, say, \verb|log| is found.

\bibliographystyle{plain}
\bibliography{simd.bib}

\end{document}
